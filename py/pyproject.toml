[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "hos"
version = "0.0.1"
description = "Python bindings for the HOS from-scratch LLM engine"
requires-python = ">=3.9"

[tool.maturin]
manifest-path = "Cargo.toml"
